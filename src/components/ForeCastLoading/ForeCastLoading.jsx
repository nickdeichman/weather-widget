import React from 'react';
import Skeleton from '@mui/material/Skeleton';

const ForeCastLoading = () => {
  return <Skeleton variant='rounded' width={310} height={100} />;
};

export default ForeCastLoading;
